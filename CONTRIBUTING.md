# 貢献のしかた

**誤りの指摘が一番ありがたいです。** 遠慮なく投げてください。

## 何を受け付けるか

| 歓迎 | 内容 |
|---|---|
| ✅ 事実の誤り | 数式が違う・アルゴリズムの説明が違う・用語の取り違え |
| ✅ 古い記述 | ライブラリのAPI変更、シラバス改訂で扱いが変わった箇所 |
| ✅ 出典の不備 | リンク切れ、出典に書いていないことが本文にある |
| ✅ 動かないコード | 掲載コードが実行できない |
| ✅ わかりにくさ | 「この説明だと逆に読める」など |
| ✅ 未執筆の概念 | 台帳にあるのに書かれていないもの |

| お断り | 理由 |
|---|---|
| ❌ 試験問題・過去問・想定問題 | 受け取れません。**Issue に書かれた場合も削除します** |
| ❌ 他社教材からの転記 | 著作権の問題。出典が一次情報でないものは取り込めません |
| ❌ 「E資格に出ますか？」の質問 | 答えられません。公式にご確認ください |

## Issue の立て方

[新しい Issue](../../issues/new/choose) からテンプレートを選んでください。
サイトの各ページ下部にある「GitHub で報告する」ボタンからだと、対象ファイルが自動で入ります。

## Pull Request の出し方

1. `content/<slug>.md` を直す
2. ローカルで検証する（**ネットワーク不要**）

   ```bash
   python3 scripts/validate.py content/<slug>.md
   ```

3. PR を出す。CI が同じ検証をかけます

### 出典のルール（ここだけは厳しいです）

**本文に書いてよいのは、`sources` に挙げた一次情報で裏が取れることだけです。**

- 出典に使えるドメインは `scripts/validate.py` の `SOURCE_ALLOWLIST` に列挙してあります（arXiv・公式ドキュメント・学会論文誌など）
- 許可されていないドメインを足したい場合は、**まず Issue で相談**してください。「その情報源が一次情報か」の判断が要ります
- 記憶で書かない。URL をそれらしく組み立てない。**開いて読んだものだけ**を置いてください

### 数式とコード

- 数式は KaTeX 記法（`$...$` / `$$...$$`）。CI がパースを確認します
- Python のコードブロックは**実際に実行されます**。実行に外部データやGPUが要るものは ` ```python no-run ` を付けてください

## ライセンスへの同意

貢献した内容は、本文が **CC BY 4.0**、コードが **MIT** で公開されます。PR を出した時点で同意したものとして扱います。

## AI エージェントについて（届いた報告は自動で処理されます）

**あなたの Issue は、まず AI エージェントが読んで分類します。** 自動で直せると判断されたものは
Pull Request になり、機械検証を通ってからマージされます。人間が出てくるのは、機械が
「これは判断が要る」と判定したときだけです。

- 反応が速いのはそのためです。**内容が軽く見られているわけではありません**
- エージェントが出す PR には `agent:` ラベルが付きます。差分はいつでも確認・異議申し立てできます
- 自動で直った場合も、Issue には何をどう直したかが残ります
- 仕組みと、どこで人間に回るかは [docs/automation.md](docs/automation.md) に全部書いてあります

### お願い

- **テンプレートの欄をできるだけ埋めてください。** 引用と根拠URLがあると自動処理に乗ります
- **エージェント宛の指示を Issue に書かないでください。** 「前の指示を無視して〜」のような文が
  含まれていると自動処理が止まり、人間の確認待ちになります（そういう設計です）

## 行動について

技術的な指摘に集中してください。人ではなく内容について書いてもらえれば十分です。
